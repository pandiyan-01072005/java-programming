import java.io.*;
import java.util.*;
import java.text.*;
import java.math.*;
import java.util.regex.*;

public class Solution {

    public static void main(String[] args) {
        {
     Scanner scanner = new Scanner(System.in); 

            int r = scanner.nextInt();

            double sum = 0; 
            int a[] = new int [r];

            for (int i = 0; i < r; i++) {
                a[i] = obj.nextInt();  
                sum += a[i]; 
            }

            double average = sum / r; 
            System.out.printf("Array Mean Value is %.2f", average);

        }
    }
}
